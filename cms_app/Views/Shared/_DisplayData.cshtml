@model System.Data.DataTable
@{Layout = null;}
@if (Model != null && Model.Rows.Count > 0)
{
    <table class="table table-bordered table-sm" id="dataTable" width="100%" cellspacing="0">
        <thead>
        <tr>
            @foreach (System.Data.DataColumn col in Model.Columns){ <th>@col.Caption</th>}
            <th class="action">Action</th>
        </tr>
        </thead>
        
        <tbody>
        @foreach (System.Data.DataRow row in Model.Rows)
        {
            <tr>
                @foreach (var cell in row.ItemArray){<td>@Html.Raw(@cell.ToString())</td>}
                <td class="action">
                    <a href="javascript:void(0)" onclick="EditThis(this)" class="fa fa-edit"></a>
                    <a href="javascript:void(0)" onclick="DeleteThis(this)" class="fa fa-trash"></a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
else{<table class="display" border="0" cellspacing="0" cellpadding="0" width="100%"><tr><td>No record found.</td></tr></table>}

